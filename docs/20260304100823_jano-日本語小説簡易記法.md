# jano-日本語小説簡易記法

`jano`は`Japanese Novel`が語源。
日本語小説を簡易記法により執筆・読書・印刷できるツールを目指す企画名である。

<!-- more -->

# 背景

日本語で小説を書きたい。だが既存の方法では問題がある。HTMLは冗長だし、Markdownではルビ記法がないし、青空文庫やカクヨムなど各小説サイト形式は効率が悪い（自動化ツールがなかったり、再利用禁止だったり、各サイトでアカウントを作成したり、変更される規約を頻繁に理解せねばならないなど余計な作業や束縛が多すぎる）。

かつてJavel、Jaml、Doml、JaDocなど同類の大規模企画をしたが、要件に基づき汎用性・抽象度を高めすぎて実装が進まなかった。そこで今回は実装を優先する。但し全体像を把握するため広めに要件を書いておく。

# アーキテクチャ

開発環境は[bun][]。言語はHTML,CSS,JS。

[bun]:https://bun.com/

使用環境|機能
--------|----
CLI|相互変換(原稿、HTML、Markdown、AsciiDoc、各種小説投稿サイト形式)
Browser(WebApi)|相互変換
Browser(Lib)|各種機能（パース、レイアウト、変換）
Browser(UI)|埋め込み（執筆、読書、印刷、変換）
Browser(App:FILE)|執筆、読書、印刷、変換
Browser(App:HTTPS)|執筆、読書、印刷、変換
Browser(Ext)|執筆、読書、印刷、変換
GUI|原稿を直接画面表示する（HTML描画APIでなくOS描画APIで）

オフライン、オンライン共に可能である状況にしたい。

## 原稿

小説の原稿は基本的に単一のテキストファイルである。文字コードは`UTF8`。

拡張子|概要
------|----
`.jn`,`.jano`|原稿のプレーンテキスト
`.jd`,`.jadr`|原稿を[Compression Streams API][]の`deflate-raw`で圧縮したファイル。
`.jg`,`.jagz`|原稿を[Compression Streams API][]の`gzip`で圧縮したファイル。

[Compression Streams API]:https://developer.mozilla.org/ja/docs/Web/API/Compression_Streams_API

原稿の中には小説本文以外にもメタデータや設定を含めることができる。それを別ファイル化することもできる。以下のように。それを一つのファイルに圧縮してまとめたものが`.jg`。

### ファイル分割＆圧縮

* jano-book/
	* manuscript/				原稿や資料
		* head/					資料
			* plan/				body/の企画データ
				* logline.jn
				* pitch.jn
				* plot.jn
				* bs-sheet.jn
				* plot/
					* 0.jn
					* ....jn
				* bs-sheet/
					* 0.jn
					* ....jn
			* meta/				body/のメタデータ
				* work.jn		作品メタ情報
				* author.jn		著者メタ情報
				* setting.jn	設定（CSS等）
				* revision.jn	リビジョン（diff）
			* agree/			集計（文字数、目次など）
				* volume.jn		規模（文字数、ファイル数、バイト数など）
				* toc.jn		目次
		* body/				原稿
			* 0.jn			本文
			* ....jn		本文
	* html/					出力結果（Web本）
		* layout/
			* screen/
				* dynamic/
					* index.html			表示したディスプレイやブラウザのサイズに応じて表示
				* static/
					* 1920x1080-40.html		画面サイズ1920x1080一行あたり40字
			* paper/
				* dynamic/
					* index.html			指定した設定に応じて表示
				* static/
					* saddle-A4-V40.html	中綴じ紙面サイズA4縦書き一行あたり40字
			* section
				* dynamic/
					* index.html			指定した設定に応じて表示
				* static/
					* 0.html
					* ....html

### DBファイル（SQLite3）

`jano-book.sqlite3`のような単一ファイルにまとめる。今回は対象外とする。

### まとめる単位

単位|概要
----|----
作品|一つの作品を完結まで書いた単位
著者|一人の著者が書いた全作品を網羅する単位
本棚|一人の集者が集めた全作品を網羅する単位

今回は作品単位のみを対象とする。但し念の為に拡張子を考えておく。

単位|拡張子|概要
----|------|----
作品|`.jn`,`.jano`|原稿のプレーンテキスト
作品|`.jd`,`.jadr`|原稿を[Compression Streams API][]の`deflate-raw`で圧縮したファイル。
作品|`.jg`,`.jagz`|原稿を[Compression Streams API][]の`gzip`で圧縮したファイル。
著者|`.ja`,`.jaut`|特定の著者による全作品を網羅したファイル。`author`(著者)
本棚|`.jf`,`.jshf`|特定の集者による全作品を網羅したファイル。`shelf`(棚)

# 機能

1. parse（tokenize, lex, syntax(fence, block, inline)）
2. layout（screen, paper, section, scroll）
3. convert（jano, html, markdown, asciidoc, 各種小説形式(青空文庫、カクヨム、なろう、AP等)）
4. export（`jn`,`jd`,`jg`形式相互変換。pack/unpack）

原稿をHTMLにパースし、レイアウトすれば閲覧可能になる。原稿を書いたらそれを他形式に変換できるようにする。これにて[ISO 25010][ISO 25010 のソフトウエア品質特性の紹介]における「有効性」「利用状況網羅性」を優先達成できる。

[ISO 25010 のソフトウエア品質特性の紹介]:https://qiita.com/mmake/items/ca97c616af0762a5164d

# 計画フェーズ

計画全体はあまりに大きすぎる。そこで何段階かに分割する。

1. CLI: `jano`コマンド作成
2. DOM:
	1. ブラウザ上でリアルタイムにパースする（10万字以上の大規模でもフリーズせぬよう工夫する）
	2. Layout: 画面サイズに応じてページ分割するライブラリを実装する
3. 変換:
	1. 原稿からHTML以外の各種形式に変換可能にする
	2. 原稿以外の各種形式から原稿に変換可能にする
4. 包装:
	1. `jn`と`jd`を相互変換する
	2. `jn`と`jg`を相互変換する

# 第一フェーズ: CLI

計画の第一フェーズ`jano`コマンド作成。

[bun][]で`jano`コマンドを作る。原稿からHTMLテキストに変換する機能を実装する。

```sh
$ jano
日本語の小説を執筆・読書・印刷・変換する。v0.0.1

* 原稿をHTMLに変換する
* HTMLをPDFに変換して印刷する
* 原稿を各種形式に変換する

サブコマンド

help     ヘルプを表示する
version  バージョンを表示する
update   更新する

intro    導入する
usecace  ユースケースを表示する
arch     システム構成を表示する
example  使用例を表示する
syntax   原稿の構文を表示する

parse    原稿をHTMLに変換する
layout   HTMLをページ単位に分割する
convert  形式変換する
pack     圧縮展開する

make     プロジェクトを作成する
lint     静的検査する
build    構築する（原稿からHTML作成する等）
preview  表示する（ブラウザ）
push     アップロードする（AT-Protocol、GitHub、オンラインストレージ）
pull     ダウンロードする（AT-Protocol、GitHub、オンラインストレージ）
spread   拡散する（SNS告知、ブログ投稿、RSS更新）

詳細
jano help [サブコマンド名] で詳細閲覧可能。

ソースコード
https://...

ライセンス: AGPL
```

最終的には上記コマンドをすべて実装したい所だが、最初は`parse`のみを実装対象とする。

## `usecase`

A. 単発の仕事をする
B. 一人の著者として作品を執筆・公開する

両者の違いは外部サービス連携するためのアカウントや対象データの管理が必要か否か。
前者は通常のコマンド通り標準出力や単一ファイル出力する。
後者はパスワードなど秘密鍵となるデータも扱う。

### A. 単発の仕事をする

原稿からHTMLに変換するなど単発の仕事をする。

### B. 一人の著者として作品を執筆・公開する

原稿をGitHubにアップロードしてSNSで告知するなど一人の著者として作品を執筆・公開する。

著者や大量のデータを管理せねばならないため、プロジェクト専用ディレクトリにデータを配置する。`jano`コマンドはカレントディレクトリに`jano.yaml`があればプロジェクト用コマンドを使える。

* ~/.jano	著者ID、表示名、外部サービスアカウント。それら秘密鍵を複数一元管理する。
* some-work/
	* src/
	* dist/
	* jano.yaml		janoプロジェクト。著者ID、作品ID等。
	* README.md
* some-author/
	* works/
		* 0/
			* src/
			* dist/
			* jano.yaml		janoプロジェクト。作品ID等。
			* README.md				
	* dist/
	* jano.yaml		janoプロジェクト。著者ID。
	* README.md

# parser

主要な機能はparserとlayoutだ。その中でも肝心要がparserである。

# 簡易日本語小説パーサ

カクヨムのような簡易記法によるHTML変換パーサを実装したい。

但し一つのファイルでメタデータや全章を含めたものであり10万字以上を想定する。作品を完結できる全内容を包含するため数百万字規模も想定する。

フリーズせず迅速にページ表示したい。スクロールではなく本のようにページ遷移したい。画面サイズに応じて読みやすいフォントサイズに自動調整したい。URLにより指定箇所を迅速に表示したい。

<!-- more -->

# 構造

テキスト全体を以下三要素に分割する。

1. フェンス要素
2. ブロック要素
3. インライン要素

以下のような書式にする。

````markdown
```fence
フェンス要素。

二連続改行すらテキスト内容として解釈する。
```

ブロック要素。
一つの改行ならブロック内だが、
二連続改行すると次のブロックと解釈する。

インライン要素。漢字《かんじ》のように特定のメタ文字を使って一部分をHTML要素化する。インライン要素は内部に改行コードを含まないものである。
````

# フェンス要素

フェンス要素は、二連続改行すら内部テキストとして解釈するjano原稿構文における最大単位。

行頭に三つ以上の同じ記号が連続したものをフェンス開始位置とする。そこに続けてフェンス引数を指定する。その引数次第で、内部テキストをどのようにパースするかの文脈が決定する。そして開始行と同じ三連続記号で始まる行がフェンス終了行となる。

````markdown
```fence
フェンス要素。

二連続改行すらテキスト内容として解釈する。
```
````

フェンス記号は三つ以上の数に増やしてネスト可能である。親は子より記号数が多くあるべき。

``````markdown
````parent
親フェンス
```child
子フェンス
```
````
``````

もしフロントマターをフェンス要素で表現するなら以下。但しタイトルや著者名など必須項目にまでキー名が必要な所が冗長であるため、ブロック要素として略記したい。

```yaml
---
title: |-
	タイトル
	副題
	https://...
	https://...
catch: |-
	キャッチコピー
	サブキャッチ	
	https://...
	https://...
author: |-
	｜山田、太郎《やまだ、たろう｜タロー》
	https://...
	https://...
date: |-
	yyyy-MM-DDTHH:MM:SS
	yyyy-MM-DDTHH:MM:SS
	yyyy-MM-DDTHH:MM:SS
	yyyy-MM-DDTHH:MM:SS
category: 創作＞小説＞ラノベ
genre: ファンタジー＞異世界
tag: 最強、終末、令嬢、チーレム等
warn: 性、暴力、残酷
license: CC-BY-NC-ND
---
```

# ブロック要素

ブロック要素は文書の冒頭と末尾以外は前後に二連続改行を入れることで分割するものとする。

単一ファイルならその冒頭にはフロントマター用ブロックを必須として文書のメタデータをセットする。

```markdown
タイトル：　副題
https://...
https://...
キャッチコピー：　サブキャッチ（書籍化、コミカライズ、なんちゃら賞受賞！等）
https://...
https://...
＠著者名
https://...
https://...
yyyy-mm-ddT00:00:00〜yyyy-mm-ddT00:00:00（発案〜執筆〜校正）
yyyy-mm-ddT00:00:00〜yyyy-mm-ddT00:00:00（公開〜最終更新日）
分類（カテゴリ、ジャンル）：小説＞ラノベ＞ファンタジー
語札（タグ）：最強、終末、令嬢、チーレム等
警告（セルフレイティング）：性、暴力、残酷
免許（ライセンス）：©、CC0、CC-BY、CC-BY-SA、CC-BY-ND、CC-BY-NC、CC-BY-NC-SA、CC-BY-NC-ND

＃見出し１

一つ目のパラグラフ。
形式段落《けいしきだんらく》。

＊二つ目＊のパラグラフ。

＃＃見出《みだ》し２

パラグラフ。
```

## フロントマター


```markdown
タイトル：　副題
https://...
https://...
キャッチコピー：　サブキャッチ（書籍化、コミカライズ、なんちゃら賞受賞！等）
https://...
https://...
＠著者名
https://...
https://...
yyyy-mm-ddT00:00:00〜yyyy-mm-ddT00:00:00（発案〜執筆〜校正）
yyyy-mm-ddT00:00:00〜yyyy-mm-ddT00:00:00（公開〜最終更新日）
分類（カテゴリ）：創作＞小説＞ラノベ（児童小説、堪能小説、恋愛小説、一般文芸、文学、詩…）
傾向（ジャンル）：ファンタジー＞ハイ／ロー（異世界／現代）、ダーク、シリアス、ギャグ、のんびり、楽勝
特性（タグ）：異世界、転生、転移、学生、おじさん、令嬢、最強、終末、チーレム等
警告（セルフレイティング）：性、暴力、残酷
免許（ライセンス）：©、CC0、CC-BY、CC-BY-SA、CC-BY-ND、CC-BY-NC、CC-BY-NC-SA、CC-BY-NC-ND
```

一行目はタイトル（必須）。`：　`が含まれていれば、その前が主題、その後ろが副題。
タイトルの下行に`https://`から始まる値があれば、この文書を表示するHTMLサイトや、原稿テキストファイルへのリンクやGitHubリポジトリなど関係するURLとする。

タイトルの次はキャッチコピー（任意）。`：　`が含まれていれば、その前が主キャッチ、その後ろが副キャッチ。
キャッチの下行に`https://`から始まる値があれば、この文書に関する書籍販売URLや評価されたサイトへのリンクとする。

タイトルやキャッチコピーは、分類、語札、警告、免許と違って固定テキストをキーにしておらず位置がキーであることに注意。

行頭`＠`なら著者名（必須）。著者名はタイトルまたはキャッチの下にあるものとする。その下行に`https://`から始まる値があれば、著者のサイトやSNSなど著者に関連するサイトへのリンクとする。

`yyyy-mm-ddT00:00:00`の書式は日時（任意）。日時は著者の下にあるものとする。一行目の日時は公開前まで。二行目の日時は公開後から最終更新まで。

ここまでの項目は位置（順序）が固定であり位置によってその内容を特定する仕組みである。

分類、傾向、特性、警告、免許は任意。特定のキーワードをキーにして指定するため、位置は日付より下であれば順不同。

分類はカテゴリのこと。文書の分類。キーは`分類`または`カテゴリ`のいずれか。木構造で包含関係を`＞`か`＜`で示す。どちらの方向から始めても構わないが、必ずどちらか一方向のみ。

傾向はジャンルのこと。内容の傾向。キーは`傾向`または`ジャンル`のいずれか。分類と同じく`＞`や`＜`が使える。

特性はタグのこと。キーは`特性`または`タグ`のいずれか。どのような属性の人が好んで読むか。その属性を表す語。嫌いなものを避けるために使われることもある。流行に支配されており流行を理解していないと読み取れず書けない。対象者の年代や性別や好みによって大きく変わる。

警告はセルフレイティングのこと。読者がダメージを受けぬよう最低限の警告を行う。性、暴力、残虐にまつわる描写の有無を知らせる。

免許は文書のライセンスのこと。再配布、改変（二次創作の公開）の自由度を示す。もし免許の項目がなければ無条件で`©`であり通常通り著作権がある。もし免許の項目があればライセンス名を表記する。基本的にCC（クリエイティブ・コモンズ・ライセンス）が対象。通常の著作権を意味する`©`も含めれば8種類。それ以外に小説専用のライセンスはまだ世界に存在しないと思われる。たとえば人物や世界観など設定だけは絶対に守れ等。しかし抽象度が高く、そのための設定集を必要とし、作者の独断と偏見でしか判断できず実現は難しい。

## 見出し

```markdown
＃見出し１

＃＃見出し２

＃＃＃見出し３

＃＃＃＃見出し４

＃＃＃＃＃見出し５

＃＃＃＃＃＃見出し６
```

見出しは行頭に`＃`が`1`〜`6`個連続している時に該当する。

見出し文字の一文字目に`＃`を使いたい場合もあるかもしれない。この時は見出しメタ文字との間に全角スペースを入れる。以下のように。尚、見出し文字の先頭と末尾に全角スペースは使えない。トリムされるものとする。

```markdown
＃＃＃　＃見出し３
```

以下の書式も考えたが、所々の理由で却下した。見出しの最初に全角数字を使いたい場合は全角スペースを挟めば良い。

```markdown
＃タイトル

＃１見出し１

＃２見出し２

＃３見出し３

＃４見出し４

＃５見出し５

＃６見出し６
```
```markdown
＃３　３見出し３
```

この番号は深度ではなく順序に見えてしまうため紛らわしい。ページ番号のような順番に見えてしまう。よって数による`＃`略記法の採用は見送り`＃＃＃見出し３`のように`＃`を複数個書く表記を採用した。

### 自動採番

実際、順序についてはどうするか。見出しが確定した時点で自動採番するのが良いだろう。

となると原稿テキストでハードコーディングする訳にはいかない。また、設定によって採番アルゴリズムを変えたい。すなわち自動採番するのか、するなら開始は`0`か`1`かをユーザに設定させたい。

しかし間に`間話`があり、採番して欲しくない章がある場合も考えられる。`序章`,`間章`,`終章`,`外伝`など`１章`のように数値でないものを差し込みたい場合もあるし、数値にしたって小文字、大文字、漢数字（大字、小字）のパターンもある。あるいは丸囲み文字を使いたいかもしれない。`2章1節　山田の冒険⑦`のように複数の数が前後にある場合も考えられる。

中々に面倒である。自動化できることかも疑問だ。`{chap}{sec}山田の冒険{pg}`のように番号をプレースホルダーで表記する案も考えられる。

### slug

内部リンク用ID。これは自動採番できればそれをIDにできるだろう。

```html
<a href="#1-1">見出し1-1</a>

<h1 id="1-1">見出し1-1</h1>
```

但しこれが必要なのは同一文書ファイルに複数の見出しがある時だけだ。見出し毎に別ファイル化したり、DOMにより一つの見出し範囲ずつしか出力しないようにしていれば不要だ。しかしURLで直接指定することで、所定の見出し範囲の文書を取得できるようにしたいため、どのみちIDは欲しい。

目次（見出し一覧）にも使えるだろう。

### 副題

見出しには副題が欲しい時がある。以下のようにして書く。

```markdown
＃主題
副題
```
```html
<hgroup>
  <h1>主題</h1>
  <p>副題</p>
</hgroup>
```

しかし主題と副題の間を改行したい場合もあれば、`主題〜副題〜`のように一行で表示したい場合もあるだろう。これをどう調整するか。以下の様に書式を指定させるか？

```
hgroup: {main}<br>{sub}
```
```
hgroup: {main}〜{sub}〜
```

```html
<hgroup data-form="{main}<br>{sub}">
  <h1>主題</h1>
  <p>副題</p>
</hgroup>
```

問題はその書式をどう簡易記法で書かせるかだ。HTMLを直接書く書式すら許容するので雑な書式ではダメだ。以下のようにフェンス要素にするか？

```markdown
---setting
hgroup:
	form: {main}<br>{sub}
---
```

これまで全角文字だけで雑にメタデータを埋め込んでいたが、詳細設定に関しては半角文字で詳細に入力することになる。しかしこの書式は、ブロック、インラインとは別の「フェンス」という概念を取り入れる必要が出てくる。一気に実装難易度が高まりそうだから、一旦保留にする。

今回は`{main}<br>{sub}`という書式で固定にする。というか基本的には改行しないほうに特殊な実装をせねばならないため、以下のようにすれば主題と副題は改行されて表示されるはず。

```html
<hgroup>
  <h1>主題</h1>
  <p>副題</p>
</hgroup>
```

## パラグラフ

二連続改行の中にある一つの改行は`<br>`になる。

### 会話文

`＜`をメタ文字に使う。

小説には地の文と会話文の二種類ある。他にも引用文や遠方からの電話など異なる文脈があるが、いずれにせよ地の文と会話文は基本的に鉤括弧で区別している。この時、セリフの記法を用意すると便利か。

```markdown
＜ここにセリフを入れると左右を鉤括弧で閉じ、末尾の句点も消す。セリフとしての禁則事項とか決まりごとをやってくれる。改行は含められない。
```

```markdown
＜
ここにセリフを入れると複数行のセリフになる。

改行を含めることができる。

長い文章をセリフにするのは小説として微妙だが、かといって地の文にすると繰り返しになりくどいし、無理やり一行のセリフにすると長すぎて読みづらい。そんな場面で使う。
＜
```

HTML的には地の文と区別できる要素はない。`＜`を使わずにテキストで`「」`を使えば見分けが付かなくなってしまうだろう。

それでも会話文に`＜`を使うようにすれば、以下のように会話文の表示方法を切り替えることができるようになるかもしれない。

* 鉤括弧で会話文を示す
* CSSの吹き出しで会話文を示す
* 吹き出しをクリックして次の会話文を出す（これは無理か）

小説という文脈においては鉤括弧で会話文を示すべき。

懸念事項は、原稿では会話文が`「」`の書式ではないことか。わざわざ`＜`書式にする必要があるか疑問である。そのため実装優先度は低い。

## 場面転換

小説には以下のように場面転換や視点転換を読者に知らせるメタテキストがある。

用途|例
----|--
場面転換|`＊`,`◆◇◆◇`
視点|`山田視点――`, `とある一室――`, `時は西暦一九九Ｘ年――`
完了|`完`, `FIN`, `終劇`, `第一部　完`

これらを作品の本文にするには微妙だが、かといって書かないと読みにくい。しかし丁寧に説明すると冗長になるため、こうした略記に頼りたい場面がある。作品本文として文字数カウントなどはされたくないが、文字表示はしたい時がある。

HTMLでは`<hr>`が近い。セマンティクスとしては`Thematic Break`(話題転換)だ。

```html
<hr>
<hr class="scene" data-text="◆◇◆◇" data-align="center">
<hr class="view" data-text="山田視点">
<hr class="fin" data-text="完" data-align="right">
```
```markdonw
＊＊＊
```
```markdonw
＊＊＊｜◆◇◆◇
```
```markdonw
＊＊＊＞完
```
```markdonw
＊＊＊＜第一部　完
```
```markdonw
＊＊＊｜山田視点
```

行頭`＊＊＊`で`<hr>`に変換する。それはデフォルト設定だ。`data-text`はユーザ指定値であり無ければ`◆◇◆◇`。配置はユーザ指定であり無ければ`left`。その後ろに`＜`か`｜`か`＞`があれば配置は`left`,`center`,`right`。それらが無ければ`left`。もし`＜`,`｜`,`＞`をテキストの一文字目に使いたければ`＊＊＊　｜をメタ文字にせず一文字目に使う`のように`＊＊＊`の直後に全角スペースを入れること。

```markdonw
＊＊＊　｜、＜、＞をメタ文字にせず一文字目のテキストとして使うにはアスタリスク三連続の直後に全角スペースを入れること
```

もしフェンス記法を実装したらユーザはよく使うパターンを以下のように指定できるようにしたい。

```markdonw
---setting
thematic-break:
	default:
		text: ◆◇◆◇
		align: left	
---
```

## まえがき／あとがき

作品ではなく著者が読者に直接話しかけるメタい文脈。前回のあらすじや全然関係ない話など。作品とは関係ないが執筆に影響している話など。

読みたい人もいれば、読みたくない人もいる。本文以上に宣伝している人までいる。

一つのパラグラフで済むなら以下。メタ文字は`＠`で`まえがき`は任意テキスト。

```html
＠まえがき
三行で
前回のあらすじを
書いたりする
```

複数のパラグラフが必要なら以下のようにフェンス要素にするか。

```html
＠＠＠まえがき
著者が読者に直接話しかけるメタい文脈。

複数ブロックもOK。インラインもOK。漢字《かんじ》。

見出しはNG。大規模に書くのはダメ。
＠＠＠
```

この著者メタ文脈への対処は次の通り。

* 全文テキスト表示する(`<fieldset><legend>`のような別枠である表示をする。この概念をboradとする)
* 全文削除する（作品外は見たくない）
* 全文`<detail><summary>まえがき</summary>本文</details>`表示する（``クリックしないと本文が表示されない）

これは読者の設定次第で表示を動的変更することになるか。設定の実装は大変そうなので現状は全文テキスト表示で固定にする。

```css
section.board {
    border: 1px solid currentColor;
    border-radius: 0.5em;
}
```
```html
<section class="board">
  <h2>まえがき</h2>
  <p>本文１</p>
  <p>本文２</p>
</section>
```














# インライン要素

ルビと強調の簡易記法。

HTMLにおける`<ruby>`と`<em>`の現状確認と、その簡易記法について考察する。

1. 現状確認
2. 簡易記法の考察

# 1. 現状確認

# `<ruby>`, `<em>`

Markdownのように簡易記法で`<ruby>`, `<em>`を書けるようにしたい。

* `<ruby>`: ルビ。日本語では振り仮名。
* `<em>`: 強調。日本語では傍点(圏点)。含みを持つ。`<b>`や`<strong>`とは異なり重要さを意味した強調ではなく含みを持っていることを示す。英語圏では斜体`<i>`だが、日本語では傍点(圏点)で示すのが一般的。

## `<ruby>`

`<ruby>`は以下四要素の組合せパターンがある。

* 部品: `<rb>`, `<rt>`, `<rp>`
* 用法: 振り仮名、義訓
* 位置: 上、下、両
* 分割: モノ、熟語、グループ
* 読上: アクセシビリティ

### 部品

ルビ`<ruby>`には内部に含める部品がある。

部品|名前
----|----
`<rb>`|`Ruby Base`|親文字
`<rt>`|`Ruby Text`|ルビ文字
`<rp>`|`Ruby Parenthesis`|ルビ括弧

公式仕様[HTML Living Standard][]では既に`<rb>`は削除されており`<ruby>`にテキストノードとしてそのまま入力することになっている。しかし依然として親文字という概念は健在する。

[HTML Living Standard]:https://html.spec.whatwg.org/multipage/

ルビは親文字に対してルビ文字を付与する。ルビ括弧は、ルビ機能に非対応のブラウザで用いるフォールバック用の要素である。

```html
<ruby>超電磁砲<rp>（</rp><rt>ちょうでんじほう</rt><rp>）</rp></ruby>
```

### 用法

ルビ（和訳:`振り仮名`）には以下二つの用法がある。

用法|概要
----|----
読み仮名|漢字の正しい読み方（音読み・訓読み）を示す。
義訓|本文の漢字とは別の、意味やイメージに合わせたカタカナや言葉を読みとして当てる。

```html
<ruby>超電磁砲<rt>ちょうでんじほう</rt></ruby>
```
```html
<ruby>超電磁砲<rt>レールガン</rt></ruby>
```

どの用途であるか機械判定させるために`data`属性値を使うことも検討できる。

```html
<ruby>超電磁砲<rt data-yomi>ちょうでんじほう</rt></ruby>
```
```html
<ruby>超電磁砲<rt data-gikun>レールガン</rt></ruby>
```

### 位置

ルビの表示位置。横書きなら上、下、両方の三通り。縦書きなら右、左、両方の三通りある。デフォルトは開始位置(上/右)である。

書字方向|ルビ位置
---------|--------
横書き|上、下、両方
縦書き|右、左、両方
一般的|始、終、両方

CSSで以下のように実装する。

```css
ruby.over {ruby-position:over;}
ruby.under {ruby-position:under;}
```

HTMLでCSSを利用すると以下。

```html
<ruby class="over">超電磁砲<rt>レールガン</rt></ruby>
```
```html
<ruby class="under">超電磁砲<rt>レールガン</rt></ruby>
```

#### 両面ルビ

両方に付与するときは`<ruby>`をネストし、位置を調整する。

```html
<ruby class="over"><ruby class="under">超電磁砲<rt data-gikun>レールガン</rt></ruby><rt data-yomi>ちょうでんじほう</rt></ruby>
```
```html
<ruby class="under"><ruby class="over">超電磁砲<rt data-gikun>レールガン</rt></ruby><rt data-yomi>ちょうでんじほう</rt></ruby>
```

### 分割

ルビは分割方法によって記法が変わる。

分割|概要
----|----
モノ|親文字一字に対してルビを振る。（親文字は漢字）
熟語|熟語全体に対してルビを振る。（親文字は全て漢字）
グループ|非漢字を許容する全親文字に対してルビを振る。

分割|例
----|--
モノ|`<ruby>学<rt>まな</rt></ruby>び`
熟語|`<ruby>学校<rt>がっこう</rt></ruby>`
グループ|`<ruby>伸縮自在の愛<rt>バンジーガム</rt></ruby>`

例えば同じ`山田太郎`でも、以下のように書ける。

分割|例
----|--
モノ|`<ruby>山<rt>やま</rt>田<rt>だ</rt>太<rt>た</rt>郎<rt>ろう</rt></ruby>`
熟語|`<ruby>山田<rt>やまだ</rt>太郎<rt>たろう</rt></ruby>`
グループ|`<ruby>山田太郎<rt>やまだたろう</rt></ruby>`

モノは漢字として最も正しい。熟語は氏名を分離している。グループは氏名全部含めている。

別にどんな分割でも良いのでは？　と思うだろうが、致命的な問題がある。

#### テキスト検索

じつはテキスト検索する時は要素単位で分割されてしまう。

たとえば`山田太郎`をモノルビで書いたら以下になるが、この時これをブラウザで表示しつつ`やまだ`と検索してもヒットしない。ブラウザの種別やバージョンによっては`山田`でもヒットしない。

```html
<ruby>山<rt>やま</rt>田<rt>だ</rt>太<rt>た</rt>郎<rt>ろう</rt></ruby>
```

HTML要素とテキストノードの間すら跨げるような検索がしたい。この要件を実現するには、プレーンテキスト化したテキストと、それがHTML表示されている箇所を紐付ける必要がある。その機能実装が非常に大変そうだ。

まずルビをプレーンテキスト化した場合、親文字とルビ文字（上下両方）で最大三パターンあることになる。しかも他のルビ文字と組み合わせたら文書全体のパターン数は膨大になってしまう。よって検索キーワードが入力されたら、その語がルビに存在するかどうかを調べねばならない。だがそれ自体が難解だ。一文字に分解して存在確認するとしたら、どれほどの処理量になるか。

* 処理量＝文書内ルビ数×検索キーワード文字数

流石に多すぎる。

しかし以下のHTMLにおいて`まなぶ`という検索キーワードでヒットさせたい。どうしたものか。

```html
<ruby>学<rt>まな</rt></ruby>ぶ。
```

#### テキストコピー

ブラウザに表示されたものをマウスで範囲選択してコピーした時、どうなるか。

ブラウザの種別やバージョンによって挙動が変わる。

* ルビ文字はコピーされない
* ルビ文字もコピーされる（`<rp>`の字もコピーされる）

もし`<rp>`がなければ`山田太郎やまだたろう`となってしまう。これを解決するために`<rp>`を使う方法が考えられる。以下のようにすると`山田太郎（やまだたろう）`とコピーされる場合がある。

```html
<ruby>山田太郎<rp>（</rp><rt>やまだたろう</rt><rp>）</rp></ruby>
```

しかし、それはルビを表すメタ文字とは限らない。心の中で思っている文脈を意図する時にも`（）`が使われるからだ。

文脈まで考慮されたテキストをコピーしたい。つまりブラウザでコピーする場合は、原稿テキストをそのままコピーしたい。しかしそのような機能はブラウザに存在しない。実装する必要がある。

そのためには、範囲選択した箇所と原稿箇所を紐付ける機能が必要になる。これが大変そうだ。

#### 氏名分割

日本語では人名を複数の漢字で表しつつ、更に氏と名に分かれる。この時、氏と名の単位で熟語ルビにしたい場合もあれば、グループルビにしたい場合もある。あるいはモノルビにしたい場合もある。いずれにせよ選択肢があるため同じ親文字とルビ文字であっても分割方法が変わり、異なるHTMLになる。

#### グループルビの多様性

グループルビはモノ・熟語ルビと違い、非漢字をも許容する。例えば以下。`の`という非漢字を親文字に含めることが可能だ。また、グループルビは正式な読み方ではなく義訓を許容する。その両方の組合せが以下だ。

```html
<ruby>伸縮自在の愛<rt>バンジーガム</rt></ruby>
```

* グループルビは親文字が必ずしも「漢字のみ」とは限らない（平仮名など非漢字を許容する）
* グループルビのルビ文字は必ずしも正式な漢字の読みだけとは限らない（義訓を許容する）

### 読上

アクセシビリティを考慮する。スクリーンリーダー上ではルビ文字が読み上げられる。しかし、場合によっては読み上げられたくない場合がある。読み上げると意味が分かりにくくなったりするような場合もある。この時、読み上げを抑制したい。

例えば両面ルビ。アクセシビリティ上の混乱が発生しかねない。そこで二つある`<rt>`のうち一つを`aria-hidden="true"`で無効化する。以下のように。

```html
<ruby class="over"><ruby class="under">超電磁砲<rt aria-hidden="true" data-gikun>レールガン</rt></ruby><rt data-yomi>ちょうでんじほう</rt></ruby>
```

## `<em>`

`<em>`は含みを持つ強調だ。日本語では傍点(圏点)である。

`<b>`や`<strong>`とは異なり重要さを意味した強調ではなく、含みを持っていることを示す。英語圏では斜体`<i>`で表示されるが、日本語では傍点(圏点)で示すのが一般的だ。

ごま`﹅`による`<em>`表示はCSSで以下のように示す。

```css
em {
  font-style: normal;
  text-emphasis: filled sesame;
  -webkit-text-emphasis: filled sesame;
}
```

それをHTMLで使うと以下。

```html
「お姉様、<em>あれ</em>を使うわ」

「ええ、良くってよ」
```

## `<ruby>`と`<em>`の併用

基本的に`<ruby>`と`<em>`の併用は物理的に不可能だ。但し、Firefox 142で動作確認した所、以下のようなHTMLでルビの上に傍点の表示を確認した。

```html
<ruby><em>超電磁砲</em><rt>ちょうでんじほう</rt></ruby>
```
```html
<ruby>超電磁砲<rt><em>ちょうでんじほう</em></rt></ruby>
```
```html
<em><ruby>超電磁砲<rt>ちょうでんじほう</rt></ruby></em>
```

どうやら`<ruby>`と`<em>`の親子関係を問わず、必ず傍点がルビの上に表示されるようだ。但し`<rt>`内に`<em>`があると傍点の数は`<rt>`内字数になるし、それ以外であれば親文字の字数になる。また傍点の大きさも`<rt>`内だと小さくなるようだ。

ルビの上に傍点があるなら、ルビの数と同じであるほうが自然だ。特に上記例の場合、親文字だと４字、ルビ文字だと８字になり、まったく異なる。但し、`<rt>`内`<em>`だと傍点が小さくなり強調度合いが少ない。かといってルビより少ない４字の傍点を８字のルビの上に配置するのも不自然である。親文字の直上ならば自然だったのに。

つまり`<ruby>`と`<em>`を併用する場合は、以下のような点に配慮すべきである。

* 傍点は強調という意味の特性上、最も早く目に入る所に配置すべきである
* 傍点は字に付随する特性上、同じ字数であるべきである

これを踏まえると、次のような解決策を思いつく。

* 傍点は上に配置する（デフォルト）。対象は親文字。
* ルビは下に配置する。

すると以下のようなHTMLになる。

```html
<ruby class="under"><em>超電磁砲</em><rt>ちょうでんじほう</rt></ruby>
```

一応、傍点とルビの位置を逆転することはできた。技術的にも表示的にもFirefox142で以下の方法で確認した。ポイントはCSSの`text-emphasis-position: under;`だ。

```css
text-emphasis-position: under;
```
```css
em.under {
  font-style: normal;
  text-emphasis: filled sesame;
  -webkit-text-emphasis: filled sesame;
  text-emphasis-position: under;
}
```
```html
<ruby class="over"><em class="under">超電磁砲</em><rt>ちょうでんじほう</rt></ruby>
```

上記で、ルビを上、傍点を下に配置できた。しかし傍点の強調という意味からすれば、先に視界に入る位置に配置すべきである。よって技術的に可能であっても傍点を下に配置する合理性は無い。

### `<rt>`と`<em>`の併用は禁止

HTMLでは親文字でなくルビ文字のほうに`<em>`を付与することができる。

```html
<ruby>超電磁砲<rt><em>ちょうでんじほう</em></rt></ruby>
```

だがこの場合、上段が傍点とルビ文字の二段になってしまう。

通常、傍点やルビ文字は親文字の50%なので、二段あるとちょうど一行文の高さになってしまう。これは流石に余白が大きすぎる。よって一方向が二段となる併用は禁止とする。

もしルビに`<em>`を併用するなら、傍点とルビ文字を上下に分散する場合のみ許可する。

### ルビと傍点の同列化は禁止

ルビと傍点を同じ段に表示することも可能だ。ようはルビ文字の変わりに傍点文字を付与すれば良い。これなら一段分でルビと強調を併用できる。

```html
<ruby>前後<rt>まえ</rt>強調<rt>﹅﹅</rt>後<rt>あと</rt></ruby>
```

但し、それをする場面はレアケースであり、かつセマンティクスやアクセシビリティ上の問題がある。強調する意図としての`<em>`要素がないため、正しく意味付けできていない。

また、読み上げに関しては`aria-hidden="true"`を付与すべきだろう。この時、スクリーンリーダーがどうなるか不明だが「きょうちょう」と読み上げられなくなってしまうと思われる。

```html
<ruby>前後<rt>まえ</rt>強調<rt aria-hidden="true">﹅﹅</rt>後<rt>あと</rt></ruby>
```

上記の理由から、ルビと傍点の同列化は禁止する。実装しない。

ちなみに、もし以下のように書いたら二段になってしまう。これはHTMLの仕様と思われる。

```html
<ruby>前後<rt>まえ</rt><em>強調</em>後<rt>あと</rt></ruby>
```

## 用法・用量

`<ruby>`や`<em>`は乱用すると読みづらい文書になってしまう。適切な用法・用量は読者によって変わるため対象年齢に合わせた用量を概算しておく必要がある。

傍点の下配置は用法・用量としての合理性がない。それどころか傍点を下に配置することで、その行に要する幅が増えてしまい、全体的に余計な行間が増えてしまう。これは画面あたりの表示行数・字数を減らし、スクロール回数の増加につながってしまう。

ルビや傍点の追記は画面あたりの字数量を考慮する必要がある。乱用すれば読みづらいページ構成になってしまうだろう。

過剰な装飾は品質を落とす。良い文章は装飾でなく文章自体の構成や使う文字の秀逸さで決まる。

# 2. 簡易記法の考察

ここまで`<ruby>`と`<em>`の現状把握をした。これをもっと楽に書けるようにしたい。そのための簡易記法について考察する。

## 既存記法

既存では青空文庫、カクヨム、なろう、でんでん記法などがある。これらは規定のメタ文字を使うことで簡易記法を実現している。

今回の`<ruby>`と`<em>`が想定される場面は日本語の文章内である。つまり日本語入力を妨げないような記法にしたい。すると以下のような観点が重要になる。

* 日本語入力しやすいメタ文字を使う
* 文章内では使わない字をメタ文字にする

### メタ文字の候補

全角が望ましい。

#### 半角

半角文字。`|`,`{`,`}`等。画面占有量が少なく、半角という字幅によりメタ文字と自然言語との区別がしやすい。但し入力時は`半角／全角`を切り替えねばならず面倒。また、等幅フォントであっても半角と全角のサイズ差のせいで位置ずれが起きて原稿が読みづらくなる。

```html
|漢字{かん|じ}
```

#### 全角

全角文字。`｜`,`《`,`》`等。これらは等幅フォントであれば位置ずれが起きず綺麗に位置が整う。但しメタ文字の幅が大きいため画面占有量が半角よりも大きくなる。また、`《》`の入力は`半角／全角`せずとも良いものの、IMEによる入力変換が必要になる。`｛｝`のほうが簡単か。

```html
｜漢字《かんじ》
```
```html
｜漢字｛かんじ｝
```

メタ文字はユーザが任意に設定できるようにしたい。例えば以下のように。しかし今回はデフォルト値は固定とする。実装時は引数で変更可能にしたい。

````markdown
---setting
meta:
	ruby:
		base: ｜
		text: 《》
	em: ＊＊
---
````

とりあえず今回は青空文庫やカクヨムで採用され普及している`｜《》`を使う。

#### 流用性

* 他の小説サイトでも流用できるよう同じ記法にすべきか
* それとも更に簡易記法を考え、それを各サイト形式に変換すべきか
* 相互変換できるようにすべきか

### `<em>`

```markdown
＊ここを強調＊する。
```
```html
<em>ここを強調</em>する。
```

### メタ文字

強調のメタ文字は`＊`だが以下の懸念点がある

* `＊`は場面転換や注釈でしばしば使われる。果たしてメタ文字にして良いものか（共用は可能だが少し紛らわしいかも？）
* かといって半角`*`にすると入力時に切替が面倒だし、Markdownの太字`<b>`と重複する
* カクヨムのように`《《強調》》`とすると冗長だ

## `<ruby>`

親文字が漢字で、かつ`《》`の中にルビ文字を入れる方法。

```html
漢字《かんじ》
```
```html
<ruby>漢字<rt>かんじ</rt></ruby>
```

親文字が非漢字でも良いよう親文字の開始位置をパイプ`｜`で指定する方法。

```html
｜ABC《えーびーしー》
```
```html
<ruby>ABC<rt>えーびーしー</rt></ruby>
```

しかし既存の記法では以下が不可能だ。

* 下ルビ
* 両面ルビ
* 分割ルビ
* em併用ルビ

### 下ルビ

ルビ文字の先頭に`｜`を付与する。

```markdown
漢字《｜した》
```
```html
<ruby class="under">漢字<rt>した</rt></ruby>
```

```markdown
｜KANJI《｜した》
```
```html
<ruby class="under">KANJI<rt>した</rt></ruby>
```

### 両面ルビ

```markdown
漢字《うえ｜した》
```
```html
<ruby><ruby class="under">漢字<rt>した</rt></ruby><rt>うえ</rt></ruby>
```

両面ルビの場合、以下のパターンが考えられる。これをどう制御するか。

* 下ルビのほうは読み上げられないようにする
* 上ルビのほうは読み上げられないようにする
* 両方読み上げる

基本的には「読み上げを考慮しない」ことにする。理由は以下。

* 両方読み上げられると冗長ではあるが、表記内容を全て視聴できる
* 読み上げを削除するコードを書くと冗長になるため削除する

文字としては見たいけど、読み上げはされたくない。そんな場合もあるはずだ。しかしそれには非対応とする。理由は「簡易記法」が簡易でなくなってしまいそうだから。

### 分割ルビ

グループルビではなく、分割ルビ（モノルビ、熟語ルビ）にする方法。分割ルビは複数の`<rt>`を作る。区切り位置には読点`、`を使う。以下の二通りある。

* モノルビ：親文字に読点がなく、ルビ文字に読点がある
* 熟語ルビ：親文字に読点があり、ルビ文字に読点がある

#### モノルビ

```markdown
山田太郎《やま、だ、た、ろう》
```
```html
<ruby>山<rt>やま</rt>田<rt>だ</rt>太<rt>た</rt>郎<rt>ろう</rt></ruby>
```

#### 熟語ルビ

```markdown
｜山田、太郎《やまだ、たろう》
```
```html
<ruby>山田<rt>やまだ</rt>太郎<rt>たろう</rt></ruby>
```

読点があるせいで先頭のパイプを必要としてしまう。パイプをなくせばルビ文脈でなく自然文章の読点と区別がつかなくなってしまう。故に熟語ルビは親文字がすべて漢字でもパイプを必要とする。

尚、以下のようにしてしまうと別々の`<ruby>`要素になってしまう。一人の人名を表すためには一つの`<ruby>`要素にしたい。よって熟語ルビを使う。

```markdown
山田《やまだ》太郎《たろう》
```
```html
<ruby>山田<rt>やまだ</rt></ruby><ruby>太郎<rt>たろう</rt></ruby>
```

### em併用ルビ

`<em>`単体は以下だ。

```markdown
＊ここを強調＊する。
```
```html
<em>ここを強調</em>する。
```

これを`<ruby>`と併用する。

* 傍点
	* 対象: 親文字／ルビ文字
	* 範囲: 開始〜終了

さて、ルビと傍点には上記のような詳細がある。しかしそれらを考慮すると、以下のような問題が起きる。

* ルビ文字に傍点をつけると同じ方向に二段必要になってしまう（両面ルビより幅広になってしまいやすい。画面占有率が無駄に高すぎる）し、傍点が小さすぎて強調されにくい
* 範囲に毎回開始と終了をつけると冗長（よって終了位置のメタ文字は省略可能とする。つまり開始位置だけにあり終了位置になければ最後の文字までがemとする）

このことから、傍点は親文字にのみ付与可能。ルビには傍点を付与できないものとする。もし`｜漢字《＊かんじ》`のように表記しても、ルビ文字の一文字目として`＊`が使われるだけだ。

#### 親文字に傍点をつける

```markdown
｜＊漢字《かんじ》
```
```html
<ruby class="under"><em>漢字</em><rt>かんじ</rt></ruby>
```

em併用rubyは必ず下ルビになる。emは強調という特性上、傍点が最初に目に入るべきだから。ルビは優先度が下がり下ルビになる。

`＊`で囲うと終端位置を示せる。

```markdown
｜＊漢＊字《かんじ》
```
```html
<ruby class="under"><em>漢</em>字<rt>かんじ</rt></ruby>
```

基本的には全親文字が対象とすれば、以下のように短縮表記できるはず。

```markdown
＊漢字《かんじ》
```

### 組合せ

両面＋分割ルビの組合せだけ可能である。em併用できないのは、上下段のいずれかが二段以上になるから。

両面|分割|em併用|可
----|----|------|--
❌|⭕|⭕|⭕
⭕|❌|⭕|❌
⭕|⭕|❌|⭕
⭕|⭕|⭕|❌

### 分割＋em併用ルビ

#### 親全体em

```markdown
＊新垣、結衣《あらがき、ゆい》
```
```html
<ruby class="under"><em>新垣</em><rt>あらがき</rt><em>結衣</em><rt>ゆい</rt></ruby>
```

#### 親一部em

```markdown
｜新＊垣、結＊衣《あらがき、ゆい》
```
```html
<ruby class="under">新<em>垣</em><rt>あらがき</rt><em>結</em>衣<rt>ゆい</rt></ruby>
```

`＊`の範囲指定は`、`の境界を超越可能。

残念ながら指定範囲は一箇所のみ。複数ヶ所の開始と終了を設定することはできない。複数可能にすると複雑になりすぎて「簡易記法」の守備範囲を超えるから。よって`＊`が三つ以上あれば不正。

#### 両面＋分割ルビ

両面ルビ＋分割ルビ。以下のような要件がある。氏名で分割しつつ、両面ルビを使いたい場合がある。ポイントは親文字のほうにも区切り文字が必要なことと、ルビ文字の片方にだけ同数の読点が必要なことだ。

```markdown
｜新垣、結衣《あらがき、ゆい｜ガッキー》
```
```html
<ruby class="under">
  <ruby class="over">新垣<rt>あらがき</rt></ruby>
  <ruby class="over">結衣<rt>ゆい</rt></ruby>
  <rt>ガッキー</rt>
</ruby>
```

もし親文字に非漢字があり、ルビ文字に読点があれば、非漢字の親文字は読点の機能も併せ持つ。つまり熟語ルビになる。

```markdown
｜伸縮自在の愛《しんしゅくじざい、あい｜バンジーガム》
```
```html
<ruby class="under">
  <ruby class="over">伸縮自在<rt>しんしゅくじざい</rt></ruby>
  の
  <ruby class="over">愛<rt>あい</rt></ruby>
  <rt>バンジーガム</rt>
</ruby>
```

この時、親文字の非漢字`の`だけは`<ruby>`文字がネストせず、テキストノードである。しかしそれは`<rt>バンジーガム</rt>`の親文字の一字である。

両面＋分割ルビが実現する条件は以下。違えば例外発生か読点をルビ文字にして警告表示する？

* 親文字とルビ文字の読点数は同じであるべき
* 読点は片方のルビ文字のみ許容する

##### 禁止：両ルビ読点

もし上下両方ルビ文字に同数の読点があれば、二つのルビ要素に分割すべきだ。すなわち以下のようにすべきだ。

```markdown
｜伸縮自在の愛《しんしゅくじざい、あい｜バンジー、ガム》
```
```html
<ruby>
  <ruby class="over">伸縮自在<rt>しんしゅくじざい</rt></ruby>
  <rt>バンジー</rt>
</ruby>
の
<ruby class="under">
  <ruby class="over">愛<rt>あい</rt></ruby>
  <rt>ガム</rt>
</ruby>
```

だがそれは以下のように表記したのと同じだ。

```markdown
伸縮自在《しんしゅくじざい｜バンジー》の愛《あい｜ガム》
```

本来「バンジーガム」という義訓は「伸縮自在の愛」という全部の語が揃って一つだからグループルビにすべきであり、分割して熟語ルビにすべきではない。

これらのことから、二つのルビ要素に分割する記法は作るべきでないと考えられる。

仮にグループルビにすべきルビ文字が上下共に存在しないなら、それらはそれぞれ二つの別々の両面ルビとして表記すべきである。それを略記すべく上下両方ルビに読点を含める記法を用意すると、グループルビがある場合との区別がつきにくくなる。それはルビの意味が読み取りにくくなってしまう。よって実装しないものとする。

つまり親文字に非漢字があればその字を読点位置と仮定するも、ルビ文字に読点がなければグループルビになるし、同数の読点があれば熟語ルビになる。但し両面ルビの場合は両方にルビがあると不正であり、片方にのみ読点がある形のみ許容すべきだ。

#### 禁止：両面＋em併用ルビ

両面＋em併用ルビは禁止だ。

理由は上段か下段が二段になってしまい画面占有率が高まりすぎることと、傍点の対象をルビ文字にすべきになり、その影響で傍点が小さくなり強調が弱まるから。かといって親文字に傍点を当てるとルビ文字と字数が合わないため奇妙な見え方になってしまい不適切だから。

以下のように両面ルビであれば、`＊`があってもem併用ルビにはならないこと。

```markdown
｜＊漢字《うえ｜した》
```
```markdown
｜漢字《＊うえ｜した》
```
```markdown
｜漢字《＊うえ｜した》
```
```markdown
｜漢字《うえ｜＊した》
```
```markdown
｜漢字《＊うえ｜＊した》
```
```markdown
｜＊漢字《＊うえ｜＊した》
```

### 禁止：両面＋分割＋em併用ルビ

両面とem併用は二段になるため禁止。

# 割注

```markdown
〔割注。この中のテキストを50%サイズで上下二段に分割する。〕
```

割注は前後に括弧を書くか否かを決めることができる。

````markdown
---setting
warichu: （）
---
````

# エスケープ

メタ文字のエスケープについて。

jano構文を説明する文書を書きたい時、メタ文字をそのまま出力したい。しかし通常通りに書くとHTML変換されてしまう。この問題を解決するのがメタ文字のエスケープだ。

どのような表記によってエスケープを実現すべきか。まずはそれを考えてみる。

よくあるのがメタ文字の前に`\`や`¥`を入力する方法だ。対象のメタ文字が一字なら最善だろう。だが対象メタ文字が増えると面倒だし字数も増えてしまう。

半角文字を使うことでエスケープするようにしてはどうか。つまり以下だ。

全|半
--|--
`＊`|`*`
`｜`|`|`
`《`|`{`
`》`|`}`
`、`|`,`
`＠`|`@`
`＃`|`#`

だがそれだと今度は半角文字の出力ができなくなってしまう。基本的には日本語の小説では使わない文字のはずだから大きな問題にはならないはずだ。しかし使える文字を大きく制限してしまうのは問題だ。ツールによって自由度が奪われるのは好ましくない。

半角バッククォートを使う案はどうか。

`` `ここにエスケープしたい字を含んだ文字列を入れる` ``。バッククォートは記号数を増やせばネストできる。これにて半角バッククォート自体のエスケープも可能になる。

だが、もし一文字だけエスケープしたい場合は面倒だ。`` `｜` ``のように囲わねばならない。もしバッククォート自体なら``` `` ` `` ```のように書かねばならない。

よってエスケープ対象が一字の場合は、直前に`\`付与する方法のほうが好ましい。

改行コードを含めたエスケープ領域を作りたい場合は以下のようにフェンス要素にすべきだ。

``````markdown
```escape
＃見出し

このフェンス内はパースされない。pre-code要素になる。

pre要素にすると自動折返しもされなくなってしまうためCSSで折り返されるようにする。また、pre要素の表示はjano原稿ソースコードとして扱われるべきだ。それに相応しい表示方法を考えるべきだ。
```
``````
``````markdown
```escape preview
＃見出し

このフェンス内はパースされない。pre-code要素になる。

この原稿コードのパース結果を隣にプレビューする。フェンス引数にpreviewがある時そうなる。
```
``````
``````markdown
```escape this-html-preview
＃見出し

このフェンス内はパースされない。pre-code要素になる。

この原稿コードのパース結果を表示する。フェンス引数thisは原稿コード、htmlは原稿をパースしたHTMLテキスト、previewはHTML描画結果を意味する。これらの取捨選択や順序は任意に可能。
```
``````

半角バッククォートは半角文字であり、これまでのメタ文字が全角であることと統一性が取れていない。だが、そこはあえてそうする。区別がつくように。メタ文字のエスケープはレアケースだし問題ないだろう。

結論、エスケープは以下三つの方法を実装する。

* 半角バックスラッシュ`\`をメタ文字の前に付与する
* インライン要素としてバッククォートで囲む
* フェンス要素としてバッククォートで囲む

# 実装（parser）

```tree
jano-parser/
	dist/
		bun.js
		browser-esm.js
		browser-script.js
		cli.js
	test/
		bun.js
		browser-esm.js
		browser-script.js
		cli.js
	src/
		token/
		syntax/
			fence/
				escape.js
				setting.js
			block/
				front-matter.js
				heading.js
				paragraph.js
				thematic-break.js
			inline/
				escape.js
				em.js
				ruby.js
		tokenizer.js
		lexer.js
		parser.js
	README.md
```
```html
<script src="dist/browser-script.js"></script>
```
```js
const html = jano.lib.parse(原稿テキスト／URL／ファイルパス／HTMLElement（textarea,pre,pre-code等）, options);
```

```sh
$ jano-parse
日本語小説用の簡易構文をHTMLに変換する。v0.0.1

使用例
jano-parse './原稿.jn' './出力.html'
cat '原稿.jn' | jano-parse
cat '原稿.jn' | jano-parse > output.html

サブコマンド
help     ヘルプを表示する。
version  バージョンを表示する。
update   更新する。
intro    導入する。
syntax   原稿の構文を表示する。
example  原稿とHTMLと結果を包含したHTMLファイルを出力する。

ソースコード
https://...

ライセンス: AGPLv3
```



# 実験メモ

```html
<style>
ruby.over {ruby-position:over;}
ruby.under {ruby-position:under;}
em {
  font-style: normal;
  text-emphasis: filled sesame;
  -webkit-text-emphasis: filled sesame;
}
em.under {
  font-style: normal;
  text-emphasis: filled sesame;
  -webkit-text-emphasis: filled sesame;
  text-emphasis-position: under;
}
</style>

<ruby><ruby class="under">超電磁砲<rt data-gikun>レールガン</rt></ruby><rt data-yomi>ちょうでんじほう</rt></ruby>

<ruby class="under"><ruby class="over">超電磁砲<rt data-gikun>レールガン</rt></ruby><rt data-yomi>ちょうでんじほう</rt></ruby>

<ruby>超電磁砲<rp>（</rp><rt>ちょうでんじほう</rt><rp>）</rp></ruby>

<ruby>山<rt>やま</rt>田<rt>だ</rt>太<rt>た</rt>郎<rt>ろう</rt></ruby>



「お姉様、<em>あれ</em>を使うわ」

「ええ、良くってよ」

<ruby><em>超電磁砲</em><rt>ちょうでんじほう</rt></ruby>

<ruby>超電磁砲<rt><em>ちょうでんじほう</em></rt></ruby>

<em><ruby>超電磁砲<rt>ちょうでんじほう</rt></ruby></em>

<ruby class="under"><em>超電磁砲</em><rt>ちょうでんじほう</rt></ruby>

<ruby class="over"><em class="under">超電磁砲</em><rt>ちょうでんじほう</rt></ruby>

<ruby class="under"><em>漢</em>字<rt>かんじ</rt></ruby>

<ruby class="under"><em>新垣</em><rt>あらがき</rt><em>結衣</em><rt>ゆい</rt></ruby>
<ruby class="under">新<em>垣</em><rt>あらがき</rt><em>結</em>衣<rt>ゆい</rt></ruby>
```


<a id="target-environment"></a>
# [対象環境](#target-environment)

* <time datetime="2026-03-04T10:08:23+0900" title="実施日">2026-03-04</time>
* [Raspbierry pi](https://ja.wikipedia.org/wiki/Raspberry_Pi) 4 Model B
* [Raspberry Pi OS](https://ja.wikipedia.org/wiki/Raspbian) buster 10.0 2020-08-20 [※](http://ytyaru.hatenablog.com/entry/2020/10/06/111111)
* [bash](https://ja.wikipedia.org/wiki/Bash) 5.2.15(1)-release

```sh
$ uname -a
Linux raspberrypi 6.12.47+rpt-rpi-v8 #1 SMP PREEMPT Debian 1:6.12.47-1+rpt1~bookworm (2025-09-16) aarch64 GNU/Linux
```
